# Quick Note

轻量 Markdown 笔记软件。目录即仓库（vault），文件保持纯 `.md`，可与 Obsidian 及
quick-daily-note 插件共用同一个仓库并同步。

技术选型与架构决策见 [docs/技术选型.md](docs/技术选型.md)。

## 进度

**M0 外壳验证（已完成）** — 详见 [docs/M0-实现说明.md](docs/M0-实现说明.md)

- Tauri v2 + React 19 + CodeMirror 6 骨架
- Rust 侧仓库操作：列举 `.md`、读取、写入；隐藏路径（`.obsidian` 等）与非 md 文件自动跳过；路径越界拒绝
- 自动保存（停止输入 1.2s）+ `Ctrl/Cmd+S`；内容未变则不触碰文件（mtime 不变）
- 界面显示换行符风格、BOM、字节数、内容 SHA-256，并提供「校验往返」按钮现场验证

**M1 Live Preview（已完成）** — 详见 [docs/M1-实现说明.md](docs/M1-实现说明.md)

- Live Preview 装饰层：标题、粗体/斜体/删除线、行内代码、链接、复选框（可点击写回源码）、
  列表圆点、分割线、引用、围栏代码块
- 表格渲染成真正的 `<table>`：单元格内的行内语法与列对齐都会解析，点击表格即可切回源码编辑
- Mermaid 流程图等渲染成图，**按需加载**（不进主包，实测 6 个 chunk 共 832 KB）；
  图定义写错时只显示错误提示，不影响编辑器
- 图片真实加载（Tauri asset 协议 + 运行时目录授权），相对路径按笔记所在目录解析；
  文件缺失时降级成标签
- **支持 Obsidian 的 wiki 语法**：`![[图片.png]]`（含 `|宽度` / `|说明` 写法）、
  `[[笔记]]` / `[[笔记|别名]]`；wiki 目标按文件名在全库查找，不是相对路径
- **内容嵌入（transclusion）**：`![[某笔记]]` 把目标笔记内容渲染进来，支持
  `#小节`（取到下一个同级标题）与 `#^块标识`（取所在段落）。嵌入内容里的原始 HTML 会被
  转义——内容可能来自同步，而这个应用能写文件，不能当可信 HTML 渲染
- **额外 Markdown 语法**：裸链接 `https://…`、行内公式 `$...$`（KaTeX，按需加载）、
  `==高亮==`、`%%注释%%`（整段隐藏）、`#标签`、callout（`> [!note]` 标题，带类型配色与图标）、
  setext 标题、手写 HTML（`<br>`/`<font>`/`<img>` 等行内标签与 HTML 块）。
  HTML 会经 DOMPurify 清洗后渲染——**清洗只作用于显示**，文件内容与同步哈希不受影响
- **Ctrl/Cmd+点击链接用系统浏览器打开**（只认 http/https；不在应用内导航，
  那会把整个应用带走）
- **主题**：浅色 / 深色 / 跟随系统，配色全部走 CSS 变量
- **文件树反映仓库真实结构**：目录（**含空目录**）与非 Markdown 文件都会列出；不含笔记的
  目录默认收起（只放附件的目录不会刷满侧栏）
- **文件管理**：新建文件夹（侧栏按钮）、**新建日记**（侧栏「＋日记」，按日期命名）、
  **新建笔记 / 重命名 / 删除**（右键菜单——新建落在选中目录或选中文件所在目录）。新建时
  名称缺 `.md` 自动补、可写 `子目录/名称`、重名自动加序号（不覆盖）、非法名称被拒绝并提示；
  落点是「当前打开笔记所在目录」，界面上会写明。重命名会**同步更新其他笔记里的 wiki 引用**
  （wiki 按文件名解析，不改就等于引用静默失效）。删除是**移入仓库内的 `.trash`**、可以找回
- **粘贴附件**：粘贴图片/文件自动保存到附件目录（默认 `attachments`，可设置）并在光标处
  插入链接。图片用 Obsidian 风格的 `Pasted image <时间戳>.png` 命名；重名自动加序号，
  **不覆盖已有文件**。链接写法可选 wiki（`![[图.png]]`，默认）或 markdown
- **文件监听**：外部程序（Obsidian、同步进程、其他编辑器）改动文件后界面自动跟上；
  有未保存编辑时弹冲突提示，不会静默覆盖
- 光标所在行显示源码；Live Preview / 源码 双模式切换（用 Compartment，保留撤销历史）
- 围栏代码块带语法高亮，语言包按需加载

**多标签页与目录（0.3）**

- **多标签页**：每个标签独立的光标、滚动位置与撤销历史（各存一份 EditorState），
  未保存内容切走再切回不丢；关闭带未保存改动的标签会**先落盘**；删除/重命名已打开的
  笔记时标签联动（改名换键、删除切到相邻标签）
- **左右分栏（Obsidian 式布局）**：文件树常驻左栏（无页签）；右侧面板承载
  "围绕当前笔记"的内容，页签在 **日记**（日历 + 待办）与 **目录**（标题层级，点击
  跳转；ATX + setext，跳过围栏代码块）之间切换；两个侧栏都可一键收起
- **目录跟随光标**：目录中实时高亮光标所在的标题（长文档自动滚动到可见）
- **自定义 CSS**：设置面板可写自定义样式（存本机），即时注入，微调 Markdown
  渲染效果（表格/callout/标题等，清空即恢复）
- **待办行对齐插件交互**：悬停行尾出现 ⋯，点开是 修改 / 复制 / 删除 菜单；
  双击文字也可直接进入行内编辑

**M2 日记与日历（已完成）** — 详见 [docs/M2-实现说明.md](docs/M2-实现说明.md)

- **按日期建日记**：标题规则「日期 + 名字」（`2026-09-15 项目周报`），日期格式与存放目录
  可配；同名直接打开、**绝不覆盖**；同一天可以有多篇（发现逻辑本来就支持）
- **ISO 周记**：`2026-W37 周记`，用 ISO 周号年（`2027-01-01` 属于 `2026-W53`，跨年不错格）
- **模板与占位符**：`{{title}}` / `{{date}}` / `{{date:格式}}` / `{{week}}` / `{{time}}`；
  未知占位符原样保留，不会被悄悄清掉
- **日历面板**（侧栏「日记」页签）：W 周记列、日记/周记圆点打点、单击切换选中日期、
  双击打开或创建、本月天数 / 连续天数 / 今日字数；**「当天日记（N）」列出该日已建的日记**
  （点名字打开，行尾 ⋯ 与右键复用文件树的菜单），**「＋ 新建」是同一天再建一篇的入口**
- **待办**：按日期分桶、录入与勾选、**复制与修改文字**（对齐插件的 updateTodoText）、
  删除打墓碑而不是物理移除；**昨日未完成一键顺延**
  （带 `[09-07 遗留]` 前缀，顺延后源日期打墓碑，不会重复顺延）
- **与 Obsidian 插件共用同一份库内配置** `quick-daily-note.json`：目录、日期格式、模板、
  待办都在里面。写回是**补丁式**的——插件专有的键（`emailAccessKey`、天气、提醒开关…）
  原样保留，键序不变；配置解析不出来时**拒绝写回**而不是拿默认值覆盖
- 外部改动库内配置（插件、同步进程）会自动跟上，自己写入产生的监听回声按哈希忽略

**M3 云同步（已完成）** — 详见 [docs/M3-实现说明.md](docs/M3-实现说明.md)

- **与 Obsidian 插件共用同一个云端仓库**（daily-sync 服务）：账号密码登录换 JWT、
  401 自动续期换新令牌（refreshToken 一次性轮换，30 天滚动），游标/分页/幂等推送照抄插件
- **哈希口径已核实并对齐**（技术选型里列为 M3 阻断项的那一条）：Obsidian 的
  `Vault.prototype.read` 是 `readFile(utf8)` 后**剥掉 BOM**、换行符不做任何规范化，
  所以同步哈希取"**已剥 BOM 的正文**"，CRLF/CR/尾随换行原样参与哈希。
  插件 `data.json` 里存着的 9 个文件哈希逐条复核一致。**磁盘上的 BOM 仍然原样保留**——
  BOM 只是不参与哈希与上传，写回依旧是字节精确的
- **冲突策略**：本地改动胜出并回推；云端删除时本地没动过就跟删（进仓库内 `.trash`，
  可找回），本地改过就复活；双边都改过时界面列出冲突文件。唯一与插件不同的地方是
  "本设备不认识这个文件"时**本地胜**——否则一次「重置同步状态」会让云端成片覆盖本地
- **本地删除会同步出去**，而且**改日记目录不等于删除**：判定时区分"文件真的没了"
  与"只是离开推送范围"，后者绝不打墓碑（否则改一次目录就会删掉云端旧目录的整批笔记）
- **待办走虚拟文件**（不落盘、不进仓库）：按 `id` 条目级合并、`updatedAt` 新者胜、
  墓碑保留 30 天；云端由网页端或 Obsidian 加/勾/删的条目会回流，合并结果回推
- **后台同步**：改动后 3 秒防抖推送、每 5 分钟一轮、启动后 5 秒首同步；状态栏显示
  同步状态与时间，点一下立即同步；游标异常时可重置游标重拉
- **凭据与状态只在本机**（应用配置目录的 `sync-state.json`，含密码与令牌，**不进仓库**）；
  状态文件坏了**不静默降级**，而是暂停同步并给出「重新读取 / 重置同步状态」两个出口
- **附件同步尚未做**（见下），但游标已把附件的版本号一起吃掉，将来接上不会漏

- **检查更新**：设置面板手动检查 GitHub Releases（发现新版本一键打开发布页）；
  应用版本随 tauri.conf 走
- **附件同步（M3 补全）**：**按引用扫，不按目录扫**——只同步被范围内笔记引用到的
  附件（`![[图.png]]` 与 `![](attachments/图.png)` 两种写法都认，围栏/行内代码里的示例
  不算），没被引用的图片不上云，不镜像无用的二进制；本地缺的图**按文件名去云端补**，
  落到服务端 `X-Attachment-Path` 指定的路径（同目录优先 → 最短路径 → 字典序，与插件
  和服务端同一套解析）；上传与下载都有每轮上限（20 个 / 50MB），被推迟的下轮继续；
  白名单 png/jpg/jpeg/gif/webp/pdf（刻意不含 svg），上限 10MB
- **附件目录配置统一**：粘贴附件目录（`pastedImageFolder`）从本机设置**迁移到库内配置**，
  与 Obsidian 插件共用同一个键、同一份文件——两边换用不用设两次，同步一开多台设备自动
  一致。旧版本存本机的自定义值会在打开仓库时一次性迁过去
- 设置面板现在**可以滚动**（内容超过窗口高度时面板内部出滚动条）

**0.4 界面重设计：从笔记软件到个人知识管理系统**

- **深色高级主题**：全套新色板（低饱和深底 + 靛蓝强调色），深色为默认；
  浅色 / 跟随系统仍可在设置中切换。卡片化布局、大圆角、层级靠底色与阴影表达、
  减少线条；按钮/页签/侧栏收起都有克制的动效
- **三栏 PKM 布局**：左 = 知识库文件管理（笔记计数、名称筛选框、图标化文件树），
  中 = 编辑器（浮起的卡片、阅读态 15px 字号、更宽松的行高与底部留白），
  右 = 日历 / 目录 / **统计** 三个页签
- **表格渲染态就地编辑**：单元格可直接点击选中、拖选复制、输入编辑
  （contenteditable plaintext-only），失焦/Enter/Tab 时整块写回源码并重新对齐管道；
  Tab / Shift+Tab 跳格、Enter 跳下一行、Esc 撤销本格、输入竖线被拦截（防止破坏表格）；
  「＋行 / ＋列」前会先冲刷正在编辑的单元格，刚敲的字不会丢
- **全局命令面板（Ctrl+K / Ctrl+P）**：笔记名即时过滤 + **Rust 侧全文搜索**
  （`search_vault`，大小写不敏感、多关键词 AND，取每文件首个命中行，回车直达该行）
  + 快捷操作（新建/保存/切模式/开关侧栏/同步/设置等）
- **知识统计页签**：笔记数 / 库容量 / 今日字数 / 连续记录卡片，近 17 周写作活动
  热力图（数据来自日记打点），任务完成率进度条，最近编辑列表（点击打开）
- **全局快捷键**：Ctrl+K 命令面板、Ctrl+N 新建笔记、Ctrl+S 保存、Ctrl+E 切实时/源码、
  Ctrl+B / Ctrl+Shift+B 收展左右栏
- **修复**：笔记末尾被底部遮挡（编辑器改为 flex 列布局，编辑器吃剩余高度；
  正文底部留白 34vh，最后一行不再贴边）；表格点击无法选中/编辑的问题
- 日历面板改为卡片化分区；右栏页签改为 日历/目录/统计；状态栏去竖线改「·」分隔；
  横幅提示改为浮起的圆角卡片

**0.4.1 桌面化与体验精修**

- **无边框窗口**：去掉 Windows 原生标题栏，顶栏即标题栏（48px，`data-tauri-drag-region`
  拖动、双击最大化）；右侧自绘 最小化 / 最大化（还原）/ 关闭 按钮——悬停关闭键红底，
  其余灰底；需要 `core:window:*` 权限（capabilities 里已声明）
- **修复检查更新报错**：ureq 的 `TlsConfig` 默认 provider 是 rustls 而本项目只编译了
  native-tls，https 请求直接 panic；现在显式指认 `TlsProvider::NativeTls`
  （并有 `#[ignore]` 的 GitHub 连通性测试守护：`cargo test --lib net:: -- --ignored`）
- **编辑器阅读排版**：正文 16px / 1.8 行高；H1 2.35em / H2 1.7em / H3 1.32em 并加大段前
  呼吸空间；隐藏行号栏；内容居中 860px
- **专注模式**（Ctrl+Shift+F / 命令面板 / 顶栏按钮）：两侧栏与状态栏退场，只留编辑器
- **代码块头部条**：起始围栏行显示「语言名 + 复制」胶囊，复制直接取代码文本；
  mousedown 即拦截，不会让代码块退回源码
- **设置面板分类化**：左侧导航（通用 / 外观 / 日记与附件 / 云同步 / 快捷键 / 关于），
  顶部搜索框按标签实时过滤，面板从右缘滑入（200ms）；「关于」承载版本与检查更新
- **文件树三视图**：知识库 / 收藏 / 最近（收藏与最近存本机，重命名/删除自动跟随）；
  笔记行悬停出现 ⋯（右键同一份菜单，新增「收藏/取消收藏」）；文件名优先占满整行，
  修改时间只在悬停时出现（不常驻占位挤窄文件名）
- **统计页签**：顶部问候卡（按时段问候 + 今日字数/连续记录摘要）；统计数字
  count-up 增长动画
- **命令面板**：笔记结果按最近修改排序并显示时间
- **视觉降噪**：应用背景加顶部微光晕染（降低纯黑压迫感）、卡片 hover 轻浮起、
  面板/表面色整体提亮半档

**M4 增强功能（已完成）** — 详见 [docs/M4-实现说明.md](docs/M4-实现说明.md)

- **粘贴识别代码语言**：`languageDetect.ts` 自插件**原样移植**（30+ 种语言、
  特征加权打分、单行高阈值防误伤），粘贴纯文本代码自动包成围栏块；已带围栏的
  代码、代码块内、识别不出的都不干预。开关（`autoDetectCodeLang`）存库内配置与插件共用
- **周回顾**：汇总某一周的日记（篇数/字数/列表）与待办（完成/未完成，墓碑不计），
  插入当前笔记光标处；输出与插件逐行一致（含 ISO 周号年、多行待办压平、
  去空白字数口径）。命令面板两个入口——本周直接插、选定周从**最近 12 周**里挑
- **定时提醒 + 系统通知**：每天到点提醒添加待办；到点检查当日未完成待办
  （一条没有就不打扰）。`tauri-plugin-notification` 走系统通知中心，
  权限不可用时静默降级为界面提示；「当天时间点已过不再补提醒」的语义
  与插件一样放在初始化标记里，全部分支有纯函数测试
- **天气**：新建日记时经 Open-Meteo（免 key）抓取，以引用行插在 frontmatter
  之后；**落盘前并入正文**（文件只写一次，没有"先建再补写"的中间状态），
  失败静默绝不阻塞日记创建。城市与开关存库内配置
- **全局背景**：仓库内图片垫在界面最底层（不透明度/模糊/亮度/对比度/位置/缩放/
  适配全可调，与插件同一套参数），顶栏/侧栏/编辑器卡片变半透明透出背景；
  背景层 `pointer-events:none` 且在内容之下，**窗口拖拽不受影响**。
  设置存本机（与插件同一种归属）；刻意**不支持视频壁纸**（内存杀手）
- **Mermaid 增强**：图表悬停出工具栏——缩放（−/百分比/＋/还原，0.05×~8×）、
  单击全屏放大（260ms 延迟避让双击编辑）、导出 SVG/PNG。
  导出**保存进仓库**（当前笔记同目录，不走浏览器下载）；PNG 导出把
  foreignObject 换成 `<text>`，文字不丢
- **图片工具栏**：本地图悬停出 复制 / 裁剪 / 重命名 / 删除。复制统一转 PNG
  进系统剪贴板；裁剪拖拽选区后**覆写原文件**（代际参数绕过 asset 缓存，
  界面立即刷新）；删除先进 `.trash` 再**精确清理各笔记里的引用**
  （wiki 嵌入 + Markdown 图片两种写法，围栏代码块内的示例不处理，
  引用删空的行整行移除）。顺带修了 Rust 侧既有 bug：附件改名缺扩展名时
  会补成 `.md`（`架构图.png` → `架构图.md`，文件直接用废），现在补原扩展名，
  且附件的 wiki 嵌入同样按文件名更新引用
- **共享配置扩展**：`quick-daily-note.json` 的自有键从 9 个扩到 15 个
  （语言识别、提醒×4、天气×2），仍是补丁式写回——插件专有键原样保留
  有测试守护。`verify-all.sh` 新增第 12 步 `verify-m4.mjs`（86 项断言）

**0.4.2 体验修复与 Obsidian 式编辑区**

- **编辑区布局 Obsidian 化**：删掉编辑器卡片容器（圆角/阴影/边距/外边距全去），
  标签栏并入编辑区（同一表面、无分割线，活动标签高亮、其余弱化），
  编辑区铺满中间剩余空间；Markdown 内容 900px 居中（窄窗格自动留边），
  **代码块按容器查询（cqw）突破阅读宽度**铺满编辑区且永不产生页面级横向滚动条，
  表格超宽时在表格内横向滚动
- **待办面板**：添加/编辑改为**多行 textarea**（Enter 提交、Shift+Enter 换行、
  随内容长高），长待办不用左右滚动编辑；行尾 ⋯ 按钮修复了"点了没反应"
  （根因：卡片 hover 的 transform 使 fixed 菜单的包含块错位，菜单飞出屏幕），
  并改为悬停时悬浮显示在文字上方、不占版面
- **智能粘贴**：剪贴板文本按文档换行符规范化（CRLF 剪贴板不再留下裸 `\r`
  角标）；带 HTML `<table>`（Excel/网页/IDE）或 TSV 的粘贴**自动转成
  Markdown 表格**（竖线转义、空单元格保留；本身就是 Markdown 表格的不动）
- **表格修复**：修了数据行 `data-row` 写死为 1 的 bug——渲染态里编辑任何数据行
  都会覆盖第一行，加行/加列后的编辑全军覆没；**Ctrl+A 两段式全选**
  （第一次选本格、第二次升级为选整张表，复制得到表格文本）；修了
  `formatTable` 右对齐输出前导冒号导致右对齐列被静默降级的既有 bug
- **打开方式设置**：「替换当前标签（Obsidian 式，默认）/ 新开标签页」，
  已打开过的笔记两种模式都切回原标签；标签栏支持**滚轮横向滚动**
- 隐形但可点击的界面元素（表格 ＋行/＋列 按钮）加 `pointer-events: none`，
  不再拦截落点恰好在它上面的点击/双击

**0.5 渲染修复与设置增强**

- **编辑区居中修复**：CM6 的默认主题在运行时注入 `.cm-content { margin: 0; flex-grow: 2 }`，
  比应用样式表**更晚进文档**，同特异性下把 `margin: 0 auto` 覆盖掉了——编辑区因此
  一直靠左（0.4.2 的「900px 居中」实际从未生效），代码块的全出血背景也随之不对称。
  现在用 `.editor-host .cm-scroller > .cm-content` 提特异性稳赢，并给「显示行号」
  留出 gutter 占位下的居中布局
- **代码块撤销「全出血」**：0.4.2 的「代码块突破阅读宽度铺满编辑区」实测观感不佳——
  宽窗格里背景带铺满整个编辑区、代码却缩在左侧，现在代码框**与正文同宽**（900px 内、
  左缘对齐，左右 14px 内边距），真正 Obsidian 式。根因同样值得记录：CM6 运行时注入的
  `.cm-line { padding: 0 2px 0 6px }` 会把行级装饰上的 padding/margin 全部吃掉，
  凡是要改行内边距的装饰类都必须提特异性（`.editor-host` 前缀）——H1–H3 的段前
  留白与引用/callout 的左内边距也是这样才真正生效的
- **顶栏响应式**：窄窗口下按梯度退场——≤1440px 隐藏搜索框的 Ctrl+K 提示，
  ≤1280px 搜索框收成图标钮（占位文字不再挤压模式切换）、隐藏保存状态胶囊
  （未保存时仍有保存钮与状态栏），≤1080px 品牌只留图标、仓库名上限收窄；
  模式切换/胶囊等文本一律 `nowrap`，不会再被挤成竖排
- **表格渲染态结构菜单**：右键单元格弹出 在上/下方插入行、删除行、左/右侧插入列、
  删除列（Obsidian 式）。表头行删除置灰、最后一列删除置灰；写回与「＋行/＋列」同一条
  冲刷管线，编辑中的单元格先提交再变换。**修了表格常驻横向滚动条**：外层 wrap 若
  `overflow-x: auto`，右缘悬挑的「＋列」钮（`right: -26px`）会算进可滚动溢出；
  现在滚动收进内层 `.cm-tb-scroll`，悬挑钮留在外层
- **快捷键可重绑定（Obsidian 式）**：「设置 → 快捷键」点命令右侧的键位再按新组合即可
  改绑（需带 Ctrl/Alt 或功能键；同一组合绑给第二条命令时原命令自动让出），可一键恢复
  默认；绑定存本机。命令面板与工具栏提示跟随当前绑定；新增默认绑定 **Ctrl+, 打开设置**。
  捕获期间全局匹配挂起，按 Ctrl+B 重绑不会顺手把文件栏切了
- **设置面板**：尺寸**固定**（660×580，切页不伸缩，小窗口内收缩适配）；
  **背景图片改为下拉框**，直接列出仓库内全部图片（png/jpg/webp/gif/bmp/avif），
  选中即启用、选「无」即关闭；新增 **「显示行号数字」开关**（默认关，gutter 显隐走
  html 类）与 **「Markdown 渲染风格」**：`默认 / Blue Topaz 风`——后者仿 Blue Topaz
  的正文观感（彩色标题层级 + 标题底线 + 蓝色粗体/行内码 + 着色表头 + 加重 callout），
  颜色经 `color-mix` 调和主题色板，深浅色下都成立，与「自定义样式」叠加时自定义优先
- **Alt+点击增强**：行内代码 Alt+点击快捷复制（不含反引号，表格单元格内同样可用）；
  图片与 wiki 链接 Alt+点击在**资源管理器中定位文件**（`revealItemInDir`，capabilities
  已加对应权限）
- **表格单元格原文编辑**：点击渲染态里含行内样式的单元格自动切换到**原文**（含
  `` ` `` / `**` 标记），写回不再静默丢失行内语法；Esc 恢复渲染态
- **搜索面板重设计**：Ctrl+F 打开浮动卡片（右上角、毛玻璃、圆角、淡入/缩小动画），
  放大镜图标 + 「当前/总数」匹配计数；上一处/下一处/替换等按钮图标化 + 中文 tooltip；
  区分大小写 / 正则 / 全词匹配为 toggle 按钮；替换行默认隐藏、点 ⇄ 展开；
  Enter 下一处、Shift+Enter 上一处、Esc 关闭。复用 `@codemirror/search` 的查询状态
  （高亮、环绕、正则/全词），只替换了 Find Bar 界面
- **PDF 导出**：命令面板新增「导出 PDF」——整篇笔记渲染成浅色打印视图后弹出系统
  打印对话框（选「另存为 PDF」）。渲染管线复用嵌入的 markdown-it（图片按仓库解析、
  wiki 语法转换），并支持 callout 卡片与 mermaid（复用会话内已渲染的 SVG）；
  修复了打印首页空白（html/body 的 `height: 100%` 在打印媒体下占满第一页）
- **行号贴近正文**：行号 gutter 跟随阅读宽度的居中偏移（负边距叠加方案），
  数字紧贴正文左缘 6px，而不是贴在窗口边缘
- **背景图开启时的内存**：去掉顶栏/状态栏的 `backdrop-filter` 毛玻璃（合成器因此
  常驻多层大纹理），换半透明底。实测（handTraining 仓库、背景图开启、长文档 +
  mermaid）：私有工作集 ~160MB；任务管理器的「工作集」口径会把共享页与整棵
  WebView2 进程树都算进来（273MB），对比请用 `measure-rss.ps1` 的私有工作集口径
- **内存**：mermaid SVG 渲染缓存上限 30 → 12（复杂图表的 SVG 串每张可达数百 KB）；
  GUI 验收新增第 14 步 `gui-uifixes.mjs` 守护上述全部行为

**0.5.1 功能完善与体验修复**

- **callout（Alert）内部完整渲染**：此前 callout 块整体接管后子节点不再下沉，
  里面的**序号（1. 2. …）**、圆点、任务框、行内代码与加粗一律以原文显示。
  现在 callout 内容与正文走同一条渲染管线：有序列表的序号上标记色（`cm-lp-olmark`，
  与圆点同风格）、任务框可勾选、行内样式照常；`>` 的隐藏与引用样式让路由
  QuoteMark 分支协同完成，且标签/`[!type]` 改为**逐行揭示**（光标落在首行才显源码，
  对齐 Obsidian），不再整块切源码
- **快捷键增强**：新增可重绑定的编辑器命令——**切换行内代码**（默认 Ctrl+`）、
  **切换代码块**（默认 Ctrl+Shift+`）、**标题 1–6**（默认 Ctrl+1..6，作用于光标行，
  再按同级别取消、其他级别直接换级；`#标签` 行不会被误改）
- **快捷键设置页重构（Obsidian 式面板）**：粘性头部（标题 + 恢复默认/导入/导出）→
  搜索（Ctrl+F 聚焦、Esc 清空，匹配名称/描述/键位）→ 全部/已分配/未分配/已修改
  分段筛选 + 轻量统计 → 按 编辑/文件/Markdown/导航/视图/其他 分组的命令列表。
  键位改为**真实键帽**质感（等宽字体、内阴影，Mac 显示 ⌘⌥⇧）；命令名拆成
  短名称 + 弱化描述（不再整段换行）；＋/↺/× 仅 hover 行时淡入；改绑进入**行内
  录入态**（实时回显组合，Enter 保存、Esc 取消），组合被其他命令占用时就地显示
  冲突警告（查看冲突 / 重新设置，不再静默抢占）；已修改的命令带轻量徽标；
  「恢复默认」支持单条与整体（整体有二次确认弹窗）；新增**配置导入/导出**（JSON）。
  面板加宽至 860px、左导航 220px、右侧独立滚动（细滚动条 hover 增强）。
  业务逻辑不变：绑定存储、多键位、捕获挂起、与全局命令的联动全部保留
- **文件复制粘贴（知识库 ↔ 资源管理器）**：右键文件/目录 →「复制」会**同时写入
  系统剪贴板**（资源管理器里 Ctrl+V 直接粘贴文件）；在左侧树**点击目录选中**（高亮）
  后 Ctrl+V 或目录右键「粘贴到此目录」，即复制进知识库（重名自动加序号）。反向同理：
  在资源管理器复制文件后，选中知识库目录按 Ctrl+V 即写入（走粘贴事件，不碰编辑器）。
  目录不能复制进自身内部
- **数据文件编辑 + 文档预览**：json/jsonc/sql/yaml/yml/toml/ini/xml/css/js/ts/py 等
  数据文件可以直接点击打开编辑（对应语言语法高亮；仅源码视图，Markdown 装饰不参与；
  保存沿用字节精确往返）；**pdf/docx/pptx/xlsx/xlsm/html/图片** 点击打开**预览窗格**——
  PDF 用 pdfjs **懒渲染**（只渲视口内页、离开视口释放画布、关闭时经加载任务销毁
  整个文档），docx 用 docx-preview 高保真排版，pptx 用 pptx-preview 平铺幻灯片
  （旧版 .ppt 会提示另存为 .pptx），xlsx 按工作表切换，HTML 走禁脚本的
  沙箱 iframe，图片走 asset 协议居中查看；预览与编辑互斥切换（点 md 关预览、
  点 pdf 开预览，对齐 Obsidian 的点击语义）。各渲染器按需动态加载不进主包。
  PDF worker 走 **vite 内联构造 + 模块级单例**（workerPort）：tauri 自定义协议下跨源加载
  module worker 会静默失败（getDocument 永远等待，表现为"预览加载失败：
  Loading aborted"或一直空白）；worker 复用同一实例——外部传入的 workerPort
  在任务销毁时不会被 terminate，此前每次打开都 new 一个导致内存持续上涨
- **知识库树排序**：目录内按类型分组排列——笔记(md) → 预览文档(pdf/docx/pptx/
  xlsx/html/图片) → 数据文件 → 其他，互不混排；同组内文件名带日期前缀的（如
  `2026-08-17 文件处理.md`）按日期先后排序（支持 2026_8_2、2026年8月2日等写法），
  带日期的条目整体排在无日期条目前面
- **预览内存策略**：预览关闭即 terminate pdf worker；关闭后 2 分钟闲置触发一次
  深度清理（dispose pptx 内嵌 echarts 图表实例等运行期产物）。渲染器模块代码
  本身受平台限制无法卸载，常驻底数属正常现象
- **导出 PDF 文件（直接落盘）**：命令面板新增「导出 PDF 文件」——笔记渲染成独立
  HTML（打印样式内联、本地图转 file:/// 地址），交给系统 Edge/Chrome 的
  `--print-to-pdf` 无头生成；保存对话框**预填上次导出目录**（记本机，跨会话），
  未导出过则落在仓库默认位置；完成后自动在资源管理器中定位文件。
  原「导出 PDF」（打印对话框流程）保留，打印样式抽成 `printStyles.ts` 单一来源
- **修复：粘贴/同步的读写竞态**：`write_attachment` 改为原子写（临时文件+改名）——
  之前直接写目标文件，文件监听会在"已创建、内容未落盘"的窗口触发事件，
  同步引擎立刻去读就弹出"文件不可访问 (os error 2)"横幅（粘贴大文件时高发）
- **修复：空状态遮罩透出**：偶尔欢迎页会叠在上一篇笔记内容上——空状态层此前是
  透明覆盖层且在 activeTab 与 openTabs 短暂错位时就会渲染。现在只在确实没有任何
  标签时渲染，且改为不透明底色
- **Tab 键对齐 Obsidian**：此前编辑器没有绑定裸 Tab，按键会把焦点移出编辑器
  （浏览器默认行为）。现在 Tab 在光标处插入缩进单位、有选区时缩进所选行；
  Shift+Tab 反缩进；表格内的 Tab/Shift+Tab 仍是"下一格/上一格"（优先级更高）
- **反斜杠转义渲染**：`\$`、`\*` 这类转义在渲染态藏掉反斜杠、保留被转义字符并回
  普通正文色（此前反斜杠一直露着，还带 escape 语法主题的橙色）；光标落在该行时
  显出源码可编辑
- **今日字数实时化（彻底修复）**：语义修正 + 实时打通。此前只统计**按文件名排序的
  第一篇**日记——同一天写了多篇（实测仓库 6 篇）时，编辑其他篇数字永远不变
  （实测卡在第一篇的 101）。现在「今日字数」= **当天全部日记的字数总和**，磁盘值
  打底；激活标签是今天**任意一篇**日记时那篇改按编辑器内容实时计算，右侧日历页签
  「今日 X 字」与统计页签同步即时更新（不等自动保存链路）。顺带修了统计卡片
  count-up 动画每次目标变化都从 0 重播的问题（打字时数字反复掉回小值）
- **行号/正文布局修复**：「显示行号」开启时行号与正文之间隔着一大段空白（宽窗口
  下 200+px）——旧的行号/正文两条 margin 公式各自引用居中偏移，在 flex 布局里
  叠加了两次。重写为「净宽为零」叠加模型：行号列 margin 抵消自身宽度后，
  正文按 (100cqw - 900px)/2 严格居中，行号右缘距正文 6px（4 种窗口宽度实测
  gap 恒为 6px、正文左右留白对称）
- **Alt+点击**：文件树的目录/文件 **Alt+点击在资源管理器中定位**（`revealItemInDir`
  会高亮它本身，右键菜单补「在资源管理器中显示」）；按住 Alt 悬停编辑区时
  WebView2 原生的「+」光标改为 pointer（`qn-alt-held` 类）；行内代码的 Alt+点击
  复制补了**语法树兜底**——源码模式或光标所在行没有渲染 chip 时也能复制
- **今日字数实时化**：激活标签是今天那篇日记时，统计页签直接按编辑器内容计算，
  不再等「自动保存 → 列表刷新 → 重读文件」的滞后链路
- **数据目录可自定义**：「设置 → 存储」展示数据/配置目录的实际位置，可选定
  自定义数据目录（指针文件 + 启动早期设置 `WEBVIEW2_USER_DATA_FOLDER`），
  重启生效，启用时自动把现有 WebView 数据搬过去
- **最近仓库下拉**：顶栏仓库胶囊改为下拉菜单，列出最近打开的仓库（本机存 8 个）
  一键切换，「选择其他仓库…」进系统目录选择
- **日记改名保护**：日记靠「文件名以日期开头」挂在日历上，改名丢掉日期前缀会让
  它从当天列表消失、还会被「新建日记」再建一篇空的。现在重命名日记时自动保留
  （或补齐）日期前缀并提示；普通笔记不受影响
- **粘贴只认真表格**：TSV 纯文本不再自动转 Markdown 表格（代码缩进、对齐文本里的
  tab 会被误拆）；只有剪贴板带 HTML `<table>`（Excel/WPS/网页复制）才转表格，
  其余按纯文本原样粘贴

尚未开始：M5 打包与体验（自动更新、托盘、自启、内存回归的常态化）；阅读视图也未实现。

## 开发

```bash
npm install
npm run tauri dev      # 开发模式
npm run tauri build    # 打包
```

需要 Rust 1.88 及以上（部分依赖要求）、Node 20+；Windows 上还需要 WebView2 运行时
（Win11 内置，Win10 多数已随 Edge 安装）。

## 核心不变量：字节精确往返

同步协议（daily-sync）用内容的 **SHA-256** 判断文件是否变化。因此本项目的硬性约束是：

> 读写链路不得对文件内容做任何隐式改写。换行符、BOM、尾随换行必须原样保留。

一旦编辑器把 CRLF 规范化成 LF、或吞掉 BOM，所有未改动的文件都会被判成"已改动"，
制造大量虚假冲突。为此：

- Rust 侧不做任何内容规范化，只剥离 BOM 并报告其存在，写回时补上；
- 编辑器用 `EditorState.lineSeparator` facet 对齐文件的换行符风格，**序列化必须用
  `state.sliceDoc()`**——`state.doc.toString()` 固定用 `\n` 拼接，会悄悄改写 CRLF 文件；
- 写入前比较内容，一致则不落盘，避免无谓地更新 mtime 触发同步连锁。

### 验收

```bash
./scripts/verify-all.sh    # 逻辑层：磁盘 ↔ 字符串 ↔ 编辑器状态
./scripts/verify-gui.sh    # 界面层：启动真实应用，用 CDP 断言 DOM 并模拟真实输入
```

`verify-all.sh` 一条命令跑完：

1. 类型检查（能抓到 Node 类型擦除测试抓不到的类型错误）
2. 重建测试仓库并记录内容哈希基线
3. Rust 集成测试：磁盘 ↔ 字符串无损（BOM、CRLF、仅 CR、混合换行、无尾随换行、路径越界），
   以及新建/重命名/删除与「配置文件可选读取」
4. 由 Rust 导出换行符探测结果，作为编辑器层测试的**地面真值**
5. 编辑器层测试：字符串 ↔ CodeMirror 状态无损
6. Live Preview 装饰层测试：语法标记隐藏、样式、小部件、表格（含单元格行内语法与对齐）
7. 额外行内语法扫描测试：公式 / 高亮 / 注释 / 手写 HTML 的边界
8. 附件命名与链接测试
9. 日记与日历的纯逻辑测试（146 项）：ISO 周号跨年、命名与发现、模板占位符、日历网格、
   统计口径、待办墓碑与顺延、**库内配置的补丁式写回**
10. 云同步的纯逻辑测试（124 项：冲突决策、状态兜底、快照合并、哈希口径，以及附件的白名单、stamp 旁路缓存与每轮下载预算）：**冲突决策的每个分支**、同步状态的深拷贝与坏值兜底、
    待办快照的稳定性与条目级合并、哈希口径与 Node `createHash` 互校、范围判定与 URL 归一化
11. 表格与大纲的纯逻辑测试：管道对齐（CJK 显示宽度）、行列增删不丢内容、单元格区间、标题提取（围栏跳过、setext 区分）
12. 比对基线，确认整个过程没有改动任何文件

`verify-gui.sh` 会先以独立 identifier 构建一个**测试变体**（WebView2 使用独立数据目录），
再启动应用，通过远程调试端口读取**渲染后的 DOM**，并用真实鼠标与键盘完成：

1. 打开/编辑/保存后 CRLF 依然完整
2. Live Preview 的渲染与模式切换（含表格、Mermaid、图片真实加载、收尾围栏压缩）
3. 点击复选框写回源码并落盘
4. 文件监听：外部改动在无编辑时自动采纳、有编辑时弹冲突提示（两个选项都可点）、
   外部新建文件出现在文件树里
5. 粘贴附件
6. 新建/重命名/删除文件与文件夹
7. 额外语法渲染与主题
8. 日记与日历（79 项）：日历结构、单击与双击的区别、当天日记栏（篇数/打开/⋯ 菜单/
   ＋新建建出第二篇）、顺延全流程、待办增删勾选落盘、双击建周记、双击建日记并断言模板展开、
   **插件专有键在整轮读写后仍在**
9. 云同步（40 项）：**对着一个桩后端**（真实的 HTTP 服务，跑完即关）配置并点「立即同步」——
   首次推送的内容 CRLF 原样、BOM 不上云而磁盘保留、范围外的文件不推、状态里记的哈希与
   云端内容同口径；拉取/采纳/墓碑删除；本地改动 3 秒防抖自动推；双边改动的冲突提示与本地胜；
   待办快照推送与云端回流（含写回库内配置）；游标吃掉附件版本号；refresh 失效 + 密码错的
   凭证提示；改回正确密码后恢复。**桩后端是刻意的**：验收要在没有网络与 Docker 的机器上
   跑得完，而这里要验的正是字节与状态码的边界

结束后自动还原被改动的文件并比对基线。**基线现在把非隐藏的 `.json` 也算在内**——
M2 之后应用会写库内配置，它被写坏（丢掉插件键）比某篇笔记被改写严重得多，
而它以前恰好不在守护范围内。

> 用独立 identifier 有两个原因：测试不再读写你日常使用的应用配置；即使本机有残留实例锁住了
> 正式数据目录，测试也能跑。副作用是 `target/release/quick-note.exe` 会变成测试变体——
> 需要正式二进制时重新跑 `npm run tauri build -- --no-bundle`。

**两层测试缺一不可**：装饰层的单元测试能精确断言区间与类名，却测不出 CM6 对插件的运行时
限制（块级装饰、替换换行符——违反会让编辑器整体白屏）；GUI 测试能发现白屏与事件失效，
但定位不到具体原因。两个层面各自抓到过对方的漏网之鱼，详见
[docs/M1-实现说明.md](docs/M1-实现说明.md)。

> 测试脚本刻意不复刻换行符探测逻辑。此前的复刻版把 CRLF 误判为 LF，却因为
> LF 分隔符下 `\r` 会作为普通字符留在行内而"通过"——属于假阳性。现在真值只有
> Rust 一个来源（`cargo run --example dump-detect`）。

### 内存基线

```bash
powershell -ExecutionPolicy Bypass -File scripts/measure-rss.ps1
powershell -ExecutionPolicy Bypass -File scripts/measure-rss.ps1 -ProcessName Obsidian
```

输出工作集、私有提交、私有工作集三个口径。**三者常给出矛盾结论**：工作集重复计算
Chromium 共享页，私有提交包含已换出到页面文件的部分，跨应用对比建议用**私有工作集**。
有多个同名实例时脚本只测最新那个（要指定用 `-ProcessId <pid>`），避免把多实例的内存
加在一起得出虚高数字。已测数据与结论见 [docs/M0-实现说明.md](docs/M0-实现说明.md)
与 [docs/M1-实现说明.md](docs/M1-实现说明.md)。

## 目录结构

```
src/                     前端（React + TypeScript）
  lib/api.ts             Rust 命令的类型化封装
  lib/editor.ts          CodeMirror 6 装配、模式切换
  lib/paths.ts           路径与 wiki 目标解析（纯函数，可单测）
  lib/attachments.ts     附件命名与链接生成（纯函数，可单测）
  lib/paste.ts           粘贴附件的接管与落盘
  lib/settings.ts        本机设置（localStorage + 订阅）
  lib/daily.ts           日记/周记的日期、命名、模板、日历网格、统计（纯函数，可单测）
  lib/todos.ts           待办模型：墓碑、顺延、旧数据迁移、云端快照与条目级合并（纯函数）
  lib/dailyConfig.ts     库内 quick-daily-note.json 的解析与补丁式序列化（纯函数，可单测）
  lib/useDaily.ts        库内配置与待办的读写编排（防抖、回声抑制）
  lib/embed.ts           内容嵌入的切片、预处理与渲染
  lib/livePreview.ts     Live Preview 装饰层（核心逻辑为纯函数，可单测）
  lib/lineEndings.ts     换行符映射（无依赖，供测试直接引用）
  lib/hash.ts            同步口径的 SHA-256（UTF-8 字节）
  lib/sync.ts            同步的纯逻辑：状态形状、范围判定、**冲突决策**（纯函数，可单测）
  lib/syncEngine.ts      一轮同步的编排：扫描 → 拉取应用 → 推送、鉴权与续期
  lib/useSync.ts         同步在界面侧的状态、配置与生命周期
  lib/table.ts           GFM 表格的解析与结构变换（管道对齐/行列增删，纯函数）
  lib/tableEdit.ts       表格编辑命令：Tab 导航、工具栏操作落到编辑器
  lib/outline.ts         大纲提取（ATX/setext，跳过围栏，纯函数）
  lib/languageDetect.ts  粘贴代码语言识别（自插件原样移植，纯函数）
  lib/weeklyReview.ts    周回顾的格式与 12 周选择器（纯函数）
  lib/reminders.ts       定时提醒的触发判定与初始化语义（纯函数）
  lib/weather.ts         Open-Meteo 的 URL/解析/文案与正文插入（纯函数 + 动态 import）
  lib/imageOps.ts        图片引用清理与 MIME 判定（纯函数）
  lib/background.ts      全局背景的 DOM 应用
  components/ImageCropDialog.tsx  图片裁剪弹窗（拖拽选区 → 覆写原文件）
  components/WeekReviewDialog.tsx 周回顾的 12 周选择弹窗
  components/OutlinePanel.tsx   目录面板
  components/FileTree.tsx
  components/CalendarPanel.tsx   日历、统计、待办
src-tauri/               Rust 侧
  src/vault.rs           仓库文件操作 + 同步扫描（范围过滤与同步哈希）
  src/net.rs             同步用的薄 HTTP 通道（不解释业务状态码）
  src/sync_store.rs      本机同步状态的原子存取（含密码与令牌，不进仓库）
  tests/roundtrip.rs     字节精确往返集成测试
  tests/create.rs        新建/重命名/删除 + 配置文件可选读取的行为测试（独立临时仓库）
  tests/sync.rs          同步哈希口径、作用域扫描、HTTP 通道（本地桩服务器）
  examples/dump-detect.rs  导出探测结果供前端测试使用
scripts/
  verify-all.sh          逻辑层验收（一条命令）
  verify-gui.sh          界面层验收（一条命令，含构建测试变体）
  verify-cm6-roundtrip.mjs / verify-livepreview.mjs / verify-inline-syntax.mjs / verify-attachments.mjs
  verify-daily.mjs       日记/日历纯逻辑（146 项断言）
  verify-sync.mjs        同步纯逻辑（124 项断言）
  verify-tables.mjs      表格结构编辑与大纲的纯逻辑
  gui-smoke.mjs / gui-edit-save.mjs / gui-livepreview.mjs
  gui-external-change.mjs / gui-paste.mjs / gui-file-manage.mjs / gui-rendering.mjs
  gui-daily.mjs          日历面板、当天日记、日记/周记创建、待办与顺延、配置写回
  gui-tabs.mjs           多标签页与目录面板
  gui-sync.mjs           云同步端到端（自带桩后端，跑完即关）
  make-test-vault.sh     生成覆盖各类边界的测试仓库
  measure-rss.ps1        内存测量（三个口径）
  dump-markdown-tree.mjs 打印 GFM 语法树，写装饰层时用来确认节点名
docs/                    设计与实现文档
```
